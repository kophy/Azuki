#ifndef __AZUKI_COMMON__
#define __AZUKI_COMMON__

#include <memory>
#include <string>
#include <utility>
#include <vector>

namespace Azuki {

using std::pair;
using std::shared_ptr;
using std::string;
using std::vector;

typedef string::const_iterator StringPtr;

};  // namespace Azuki

#endif  // __AZUKI_COMMON__
